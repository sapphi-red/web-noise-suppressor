{
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:all": "cd .. && npm run build && cd demo && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sapphi-red/web-noise-suppressor": "file:.."
  },
  "devDependencies": {
    "@typescript/lib-dom": "npm:@types/web@^0.0.52",
    "vite": "^2.8.0",
    "vite-plugin-static-copy": "^0.2.0"
  }
}
